# === App Environment ===
BASE_URL=https://id.kiotviet.vn
APP_ENV=staging

# === Credentials (không commit giá trị thật) ===
ADMIN_EMAIL=your_email@example.com
ADMIN_PASSWORD=your_password

# === Playwright Config ===
# Đặt HEADLESS=true để chạy không hiển thị browser
HEADLESS=false
